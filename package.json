{
  "name": "backend-demo",
  "private": true,
  "scripts": {
    "setup": "node setup.js",
    "dev": "concurrently \"cd backend && npx nodemon server.js\" \"cd frontend && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
